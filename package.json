{
  "name": "ease-monorepo",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/core",
    "packages/compiler",
    "packages/runtime"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared -w packages/core -w packages/compiler -w packages/runtime",
    "test": "npm test -w packages/core -w packages/compiler"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "4.0.18"
  }
}
